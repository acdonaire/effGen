[build-system]
# setuptools>=77 is required for PEP 639 license expressions (the SPDX
# `license = "Apache-2.0"` string below). Older setuptools rejects it with
# "invalid pyproject.toml config: project.license", which surfaces on
# `pip install -e . --no-build-isolation` against an env with an older
# setuptools (default isolated installs fetch a recent build backend and work).
requires = ["setuptools>=77.0.0", "wheel>=0.42.0"]
build-backend = "setuptools.build_meta"

[project]
name = "effgen"
dynamic = ["version"]
description = "A comprehensive framework for building agents with Small Language Models"
readme = "README_PYPI.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [
    {name = "Gaurav Srivastava", email = "gks@vt.edu"}
]
maintainers = [
    {name = "Gaurav Srivastava", email = "gks@vt.edu"}
]
keywords = [
    "ai", "agents", "llm", "slm", "language-models", "small-language-models",
    "tool-use", "function-calling", "prompt-engineering", "multi-agent",
    "agent-framework", "transformers", "vllm", "openai", "anthropic", "gemini",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Education",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]

dependencies = [
    # Deep Learning & Model Support
    # Upper bounds cap fast-moving majors at the last line validated for this
    # release so a fresh install never silently pulls an untested next major
    # (torch 3.x / transformers 6.x). The "latest deps" CI lane tests above these.
    "torch>=2.0.0,<3.0",
    "transformers>=4.35.0,<6",
    "accelerate>=0.24.0",
    "safetensors>=0.4.0",
    "sentencepiece>=0.1.99",
    # protobuf pinned >= patched release per CVE-2025-4565 / CVE-2026-0994
    "protobuf>=5.29.5",
    # API Model Adapters
    "openai>=1.3.0",
    "anthropic>=0.7.0",
    "google-generativeai>=0.3.0",
    "google-genai>=1.0.0",
    # Anchor the google + protobuf client stack to modern floors. Without these,
    # the google-* trio (generativeai + genai + api-python-client) — combined
    # with the protobuf>=5.29.5 CVE pin and reward-kit/fireworks-ai's overlapping
    # google/grpc requirements — lets pip backtrack through the entire
    # google-auth / google-api-core / proto-plus / grpcio-status version history,
    # exploding the resolution graph and triggering pip's `resolution-too-deep`
    # error (reproduced in CI and on a clean local install). These floors keep
    # the resolver shallow without changing the versions it ultimately picks.
    "google-auth>=2.28.0",
    "google-api-core>=2.24.0",
    "cachetools>=5.3.0",
    "proto-plus>=1.24.0",
    "grpcio-status>=1.60.0",
    "googleapis-common-protos>=1.63.0",
    # httplib2 reaches the base install through google-generativeai ->
    # google-api-python-client; 0.32.0 is the first release that clears
    # PYSEC-2026-3444.
    "httplib2>=0.32.0",
    # pyasn1 reaches the base install through google-auth -> pyasn1-modules;
    # 0.6.4 is the first release that clears CVE-2026-59885/-59886.
    "pyasn1>=0.6.4",
    # Configuration Management
    "pyyaml>=6.0.1",
    "jsonschema>=4.19.0",
    "json5>=0.9.14",
    "python-dotenv>=1.0.0",
    # Tool Support & HTTP
    "requests>=2.31.0",
    # aiohttp>=3.14.3 clears CVE-2026-34993 / CVE-2026-47265 and the six
    # advisories still open against 3.14.1, fixed across 3.14.2 and 3.14.3.
    "aiohttp>=3.14.3",
    "httpx>=0.25.0",
    # Web Search
    "duckduckgo-search>=8.1.0",
    "ddgs>=9.9.3",
    # File Operations & Code Execution
    "docker>=6.1.0",
    "nbformat>=5.9.0",
    "jupyter-client>=8.5.0",
    # Utilities
    "numpy>=1.24.0,<3",
    "pandas>=2.0.0,<4",
    "pydantic>=2.0.0",
    "tqdm>=4.66.0",
    "rich>=13.7.0",
    "click>=8.1.0",
    "colorama>=0.4.6",
    "jinja2>=3.1.0",
    # Async Support
    "asyncio-throttle>=1.0.1",
    "nest-asyncio>=1.5.8",
    # Logging & Monitoring
    "loguru>=0.7.0",
    # GPU Management
    "gputil>=1.4.0",
    "nvidia-ml-py>=11.495.46",
    # Protocol Support - MCP
    # 1.28.1 is the first release that clears CVE-2026-59950. The 2.x line
    # removes `mcp.server.fastmcp` (which the effGen MCP server is built on),
    # drops the `streamablehttp_client` alias and restructures `mcp.types`, so
    # it needs a port rather than a version bump.
    "mcp>=1.28.1,<2",
    # Performance Monitoring
    "psutil>=5.9.0",
    # Data Processing
    "tiktoken>=0.5.0",
    "scikit-learn>=1.3.0",
    # Security
    # >=50.0.0 clears the advisories open against 48.0.1, fixed across the
    # 49.0.0 and 50.0.0 releases.
    "cryptography>=50.0.0",
    # starlette pinned >= patched release (transitively pulled by fastapi).
    # 1.3.1 is the minimum that clears all currently-known advisories:
    #   - PYSEC-2026-161               (host-header injection)
    #   - CVE-2026-54282 / GHSA-jp82-jpqv-5vv3  (URL authority poisoning, fixed 1.3.0)
    #   - CVE-2026-54283 / GHSA-82w8-qh3p-5jfq  (form() limit DoS, fixed 1.3.1)
    # Upper bound caps the next major (2.x) which is untested for this release.
    "starlette>=1.3.1,<2.0",
    # API Server
    # 0.136.3 is excluded: that release was flagged malicious (MAL-2026-4750)
    # and injects an undocumented 'fastar' dependency. The <0.140 ceiling keeps
    # fresh installs on the FastAPI line validated for this release.
    "fastapi>=0.109.0,<0.140,!=0.136.3",
    "uvicorn>=0.27.0",
]

[project.urls]
Homepage = "https://effgen.org/"
Documentation = "https://effgen.org/docs/"
Repository = "https://github.com/ctrl-gaurav/effGen"
"Bug Tracker" = "https://github.com/ctrl-gaurav/effGen/issues"
Changelog = "https://github.com/ctrl-gaurav/effGen/blob/main/CHANGELOG.md"

[project.scripts]
effgen = "effgen.cli:main"
effgen-agent = "effgen.cli:agent_main"
effgen-web = "effgen.cli:web_agent_main"

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "pytest-timeout>=2.2.0",
    "pytest-forked>=1.6.0",
    # Randomized test ordering catches cross-test isolation regressions (a test
    # that only fails depending on what ran before it). Used by the
    # order-independence CI lane; see docs/guides/testing.md.
    "pytest-randomly>=3.12.0",
    "pexpect>=4.9.0",
    "black>=23.12.0",
    "isort>=5.13.0",
    "flake8>=6.1.0",
    "mypy>=1.7.0",
    "bitsandbytes>=0.46.1",
    "hypothesis>=6.100.0",
    "cyclonedx-bom>=4.0.0",
    # Local supply-chain auditing so the security gate (tests/security/
    # test_vuln_audit.py) is runnable from a dev install, matching the CI gate.
    "pip-audit>=2.7.0",
]
# vLLM pins one EXACT torch version per release, and that torch build fixes the
# CUDA runtime. A Python extra cannot express "the torch that matches your
# driver", so on a CUDA-12 box `pip install effgen[vllm]` (latest vLLM) pulls a
# CUDA-13 torch and disables the GPU. Install vLLM against your driver's torch
# build per docs/installation.md ("Installing vLLM"); engine="auto-fast" falls
# back to Transformers if the installed vLLM can't import.
vllm = [
    "vllm>=0.2.7,<1.0",
    # via vllm / mistral-common / torchvision
    "Pillow>=12.3.0",
]
mlx = [
    "mlx>=0.16.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "mlx-lm>=0.18.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
]
mlx-vlm = [
    "mlx>=0.16.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "mlx-lm>=0.18.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "mlx-vlm>=0.1.0; sys_platform == 'darwin' and platform_machine == 'arm64'",
]
rag = [
    "sentence-transformers>=2.2.0",
    "faiss-cpu>=1.7.4",
]
finance = [
    "yfinance>=0.2.31",
]
data = [
    "matplotlib>=3.7.0",
    "plotly>=5.18.0",
    # via matplotlib
    "Pillow>=12.3.0",
]
eval = [
    "rouge-score>=0.1.2",
    "nltk>=3.8.0",
]
gguf = [
    "llama-cpp-python>=0.2.0",
]
cerebras = [
    "cerebras-cloud-sdk>=1.0",
]
rss = [
    "feedparser>=6.0.11",
]
translate = [
    "argostranslate>=1.9.0",
    "langdetect>=1.0.9",
]
qr = [
    "qrcode[pil]>=7.4",
    "pyzbar>=0.1.9",
    "opencv-python-headless>=4.8.0",
    # via qrcode[pil]
    "Pillow>=12.3.0",
]
server = [
    # OIDC/JWT auth for the API server
    "authlib>=1.3.0",
    "PyJWT>=2.8.0",
    "httpx>=0.25.0",
    # Prometheus metrics exposed at /metrics (scraped by the K8s HPA)
    "prometheus-client>=0.20.0",
]
jupyter = [
    # IPython kernel + display for Jupyter magics
    "ipython>=8.0.0",
    "ipykernel>=6.0.0",
    "jupyter-console>=6.6.0",
]
lambda = [
    # Mangum: ASGI adapter for AWS Lambda + API Gateway
    "mangum>=0.17.0",
    # The Lambda handler runs the full FastAPI server app, so it needs the
    # server extra (OIDC/JWT auth + Prometheus metrics). Self-referencing keeps
    # this in lock-step with the `server` extra instead of duplicating + drifting.
    "effgen[server]",
]
tools = [
    "pytesseract>=0.3.10",
    # Pillow 12.3.0 is the first release clear of the imaging advisories
    # (PYSEC-2026-2253..-2257, -3451..-3453, CVE-2026-54058/-59197/-59198/
    # -59200/-59204). Every extra whose dependency closure reaches Pillow —
    # directly or through another package — repeats this floor, because a
    # floor declared only elsewhere does not constrain this install.
    "Pillow>=12.3.0",
]
geo = [
    "staticmap>=0.5.4",
    "Pillow>=12.3.0",
]
documents = [
    "pypdf>=6.15.0",
    "pdfplumber>=0.10.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "pandas>=2.0.0",
    # reportlab is used by tests and validation scripts to generate
    # round-trip PDF fixtures with known text; install it alongside the
    # parsers so the full doc-pipeline test suite runs end-to-end.
    "reportlab>=4.0.0",
    # via pdfplumber / reportlab
    "Pillow>=12.3.0",
]
audio = [
    "faster-whisper>=1.0.0",
    "huggingface_hub>=0.26",
    "pydub>=0.25.1",
]
youtube = [
    "youtube-transcript-api>=1.0.0",
    # 2026.7.4 is the first release that clears CVE-2026-55404 (the
    # --write-*-link options wrote unsanitized attacker-controlled paths).
    "yt-dlp>=2026.7.4",
]
groq = [
    "groq>=0.15",
]
together = [
    "together>=1.3",
    # via together
    "Pillow>=12.3.0",
]
fireworks = [
    # Cap below 0.18: fireworks-ai 0.18+ hard-pins protobuf==5.29.3/5.29.4
    # (conflicts with our protobuf>=5.29.5 CVE floor) and pulls reward-kit,
    # which hard-pins openai==1.78.1. 0.16/0.17 require only `openai` and no
    # protobuf pin, so [all] stays resolvable and CVE-safe.
    "fireworks-ai>=0.15,<0.18",
    # h2 reaches the install through fireworks-ai -> betterproto-fw -> grpclib;
    # 4.4.1 is the first release that clears PYSEC-2026-3628.
    "h2>=4.4.1",
    # via fireworks-ai
    "Pillow>=12.3.0",
]
replicate = [
    "replicate>=1.0",
]
hf = [
    "huggingface_hub>=0.26",
]
prompts-data = [
    "sqlglot>=20.0",
]
flash-attn = ["flash-attn>=2.3.0"]
vector-db = [
    "faiss-cpu>=1.7.4",
    "chromadb>=0.4.18",
    "qdrant-client>=1.7.0",
]
search = [
    "duckduckgo-search>=8.1.0",
    "google-search-results>=2.4.2",
    "google-api-python-client>=2.108.0",
    # httplib2 reaches the install through google-api-python-client;
    # 0.32.0 is the first release that clears PYSEC-2026-3444.
    "httplib2>=0.32.0",
]
cloud-secrets = [
    "boto3>=1.28.0",
    "hvac>=1.2.0",
    "azure-keyvault-secrets>=4.7.0",
    "azure-identity>=1.14.0",
]
monitoring = [
    "wandb>=0.16.0",
    # gitpython reaches the install through wandb; 3.1.58 is the first release
    # that clears the eight advisories open against 3.1.56, fixed across
    # 3.1.57 and 3.1.58.
    "gitpython>=3.1.58",
    "tensorboard>=2.15.0",
    # via tensorboard
    "Pillow>=12.3.0",
]
# ---------------------------------------------------------------------------
# Use-case extras (curated groupings for common install intents). The base
# install already excludes the heavy optional stacks (vLLM, vector DBs, OCR,
# OpenCV, spaCy/Stanza); these named extras let users pull exactly the slice
# they need instead of the everything-extra [all].
# ---------------------------------------------------------------------------
api = [
    # Hosted-inference provider SDKs (openai/anthropic/google are already in the
    # base install). Pick this when you only call cloud models, no local stack.
    "groq>=0.15",
    "together>=1.3",
    "fireworks-ai>=0.15,<0.18",
    # h2 reaches the install through fireworks-ai -> betterproto-fw -> grpclib;
    # 4.4.1 is the first release that clears PYSEC-2026-3628.
    "h2>=4.4.1",
    "replicate>=1.0",
    "cerebras-cloud-sdk>=1.0",
    "huggingface_hub>=0.26",
    # via together / fireworks-ai
    "Pillow>=12.3.0",
]
local = [
    # Run/quantize local Transformers models on GPU (torch/transformers/accelerate
    # ship in the base install; this adds the quantization + GGUF backends).
    "bitsandbytes>=0.46.1",
    "llama-cpp-python>=0.2.0",
]
grammar = [
    # Grammar-constrained (schema-guaranteed) decoding for LOCAL Transformers
    # models. With this installed, structured output is produced in one
    # constrained pass, so even weak JSON-followers (small Llama/Gemma/Phi
    # locals) emit schema-valid output instead of falling back to prompt-and-
    # validate. Entirely optional: without it, structured output still works via
    # the universal prompt+validate path (it just can't *guarantee* conformance
    # on a model that won't follow the schema). The grammar code probes the
    # concrete outlines symbols it needs, so an incompatible build degrades to
    # the fallback rather than erroring.
    "outlines>=1.0",
    # via outlines
    "Pillow>=12.3.0",
]
tools-web = [
    # Web-search / browsing tools (DuckDuckGo ships in the base install).
    "google-search-results>=2.4.2",
    "google-api-python-client>=2.108.0",
    # httplib2 reaches the install through google-api-python-client;
    # 0.32.0 is the first release that clears PYSEC-2026-3444.
    "httplib2>=0.32.0",
]
tools-docs = [
    # Document parsing tools (PDF / DOCX / XLSX).
    "pypdf>=6.15.0",
    "pdfplumber>=0.10.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "pandas>=2.0.0,<4",
    # via pdfplumber
    "Pillow>=12.3.0",
]
all = [
    # [all] is the CI / everything extra: it installs every optional dependency
    # so the documented fresh-env verification path (`pip install -e ".[all]"
    # -c requirements-all-lock.txt && pytest tests/`) runs end to end. Most users
    # want a use-case extra above ([api], [server], [rag], [local], [vllm],
    # [tools-web], [tools-docs]) instead — [all] is large and slow to resolve.
    #
    # NOTE: flash-attn is intentionally NOT in [all]. Its setup.py imports
    # torch before pip's isolated build env has torch installed (well-known
    # upstream bug), which makes `pip install effgen[all]` always fail.
    # Install flash-attn separately: see docs/installation.md.
    # Test runner dependencies are included so the documented fresh-env
    # verification path (`pip install -e ".[all]" && pytest tests/`) works.
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "pytest-timeout>=2.2.0",
    "pytest-forked>=1.6.0",
    "pexpect>=4.9.0",
    "hypothesis>=6.100.0",
    "cyclonedx-bom>=4.0.0",
    # pip-audit so the security gate (tests/security/test_vuln_audit.py) actually
    # RUNS — not skips — in the documented `pip install -e ".[all]" && pytest
    # tests/` path. Without it the core-CVE gate is silently skipped.
    "pip-audit>=2.7.0",
    "vllm>=0.2.7,<1.0",
    "faiss-cpu>=1.7.4",
    "chromadb>=0.4.18",
    "qdrant-client>=1.7.0",
    "duckduckgo-search>=8.1.0",
    "google-search-results>=2.4.2",
    "google-api-python-client>=2.108.0",
    # httplib2 reaches the install through google-api-python-client;
    # 0.32.0 is the first release that clears PYSEC-2026-3444.
    "httplib2>=0.32.0",
    "boto3>=1.28.0",
    "hvac>=1.2.0",
    "azure-keyvault-secrets>=4.7.0",
    "azure-identity>=1.14.0",
    "wandb>=0.16.0",
    # gitpython reaches the install through wandb; 3.1.58 is the first release
    # that clears the eight advisories open against 3.1.56, fixed across
    # 3.1.57 and 3.1.58.
    "gitpython>=3.1.58",
    "tensorboard>=2.15.0",
    "sentence-transformers>=2.2.0",
    "yfinance>=0.2.31",
    "matplotlib>=3.7.0",
    "plotly>=5.18.0",
    "rouge-score>=0.1.2",
    "nltk>=3.8.0",
    "llama-cpp-python>=0.2.0",
    "cerebras-cloud-sdk>=1.0",
    "groq>=0.15",
    "together>=1.3",
    # See the [fireworks] extra: <0.18 avoids the protobuf==5.29.3 pin (CVE) and
    # the reward-kit -> openai==1.78.1 chain that makes [all] unresolvable.
    "fireworks-ai>=0.15,<0.18",
    "replicate>=1.0",
    "huggingface_hub>=0.26",
    "bitsandbytes>=0.46.1",
    "datasets>=2.14.0",
    "feedparser>=6.0.11",
    "youtube-transcript-api>=1.0.0",
    # 2026.7.4 is the first release that clears CVE-2026-55404 (the
    # --write-*-link options wrote unsanitized attacker-controlled paths).
    "yt-dlp>=2026.7.4",
    "argostranslate>=1.9.0",
    "langdetect>=1.0.9",
    "qrcode[pil]>=7.4",
    "pyzbar>=0.1.9",
    "opencv-python-headless>=4.8.0",
    "pytesseract>=0.3.10",
    "Pillow>=12.3.0",
    "faster-whisper>=1.0.0",
    "pydub>=0.25.1",
    "pypdf>=6.15.0",
    "pdfplumber>=0.10.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    "pandas>=2.0.0",
    "reportlab>=4.0.0",
    "staticmap>=0.5.4",
    "sqlglot>=20.0",
    # Lambda deployment adapter
    "mangum>=0.17.0",
    # Jupyter magics
    "ipython>=8.0.0",
    "ipykernel>=6.0.0",
    "jupyter-console>=6.6.0",
]

[tool.setuptools.dynamic]
version = {attr = "effgen.__version__"}

[tool.setuptools.packages.find]
# Ship ONLY the importable effgen package. An exclude-list let sub-packages of
# top-level repo dirs (examples.*, deploy.*, tools) leak into the wheel because
# the bare names didn't match their sub-packages; an allowlist is leak-proof.
include = ["effgen*"]
exclude = ["tests*", "examples*", "docs*", "scripts*", "deploy*"]

[tool.setuptools.package-data]
effgen = [
    "prompts/templates/*.yaml",
    "prompts/templates/*.json",
    "config/schemas/*.json",
    "config/schemas/*.yaml",
    "config/*.yaml",
    "config/*.json",
    "models/_data/*.json",
    "cli/_templates/plugin/*.tmpl",
    "py.typed",
    "dashboard/static/*.html",
    "dashboard/static/*.js",
    "dashboard/static/*.css",
    "playground/static/*.html",
    "playground/static/*.js",
    "playground/static/*.css",
    "webui/static/*.js",
    "webui/static/*.css",
]

# Black - Code Formatter
[tool.black]
line-length = 100
target-version = ['py311', 'py312', 'py313']
include = '\.pyi?$'
extend-exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | migrations
)/
'''

# isort - Import Sorter
[tool.isort]
profile = "black"
line_length = 100
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
skip_glob = ["*/migrations/*", "*/build/*", "*/dist/*"]
known_first_party = ["effgen"]
known_third_party = [
    "torch",
    "transformers",
    "openai",
    "anthropic",
    "google",
    "yaml",
    "docker",
    "pytest",
]

# mypy - Type Checker
[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
disallow_incomplete_defs = false
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
# Disabled: `# type: ignore` comments may appear "unused" on one Python/lib
# version but genuinely needed on another (optional-dep stubs, Python-version
# gated code). Enforcing this cross-version is flaky, so we trust the comment.
warn_unused_ignores = false
warn_no_return = true
# Unreachable-code warnings flap across Python/library versions (e.g. a sys.version
# branch mypy proves dead on one interpreter is live on another). Disabling here;
# dead code is caught by ruff (F) and tests.
warn_unreachable = false
strict_equality = true
show_error_codes = true
show_column_numbers = true
pretty = true

# Per-module options
[[tool.mypy.overrides]]
module = [
    "vllm.*",
    "transformers.*",
    "anthropic.*",
    "google.generativeai.*",
    "duckduckgo_search.*",
    "docker.*",
    "faiss.*",
    "chromadb.*",
]
ignore_missing_imports = true

# Fully annotated modules: every function signature must stay complete. Extend
# this list as more subpackages reach full signature coverage.
[[tool.mypy.overrides]]
module = [
    "effgen",
    "effgen.completion",
    "effgen.errors",
    "effgen._env",
    "effgen._logging_setup",
    "effgen.cache.*",
    "effgen.dashboard.*",
    "effgen.playground.*",
    "effgen.presets.*",
    "effgen.webui.*",
]
disallow_untyped_defs = true
disallow_incomplete_defs = true

# pytest - Testing Framework
[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
# NOTE: Coverage is intentionally NOT enabled in the default addopts. Enabling
# `--cov` globally made `pytest --collect-only` spin up the coverage machinery
# and, worse, made two parallel pytest shards race the single default `.coverage`
# SQLite file ("no such table: meta"). Coverage is now opt-in: pass `--cov=effgen
# --cov-report=...` on the jobs that want it, or use `scripts/run_coverage.sh`
# (unique COVERAGE_FILE per shard + `coverage combine`). See docs/guides/testing.md.
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--tb=short",
    # Keep ordering fixed by default so every lane is reproducible even when
    # pytest-randomly (in the dev extra) is installed. The dedicated
    # order-independence lane shuffles explicitly; to fuzz ordering locally,
    # drop this via `-o "addopts=" -p randomly`. See docs/guides/testing.md.
    "-p", "no:randomly",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "gpu: marks tests that require a GPU",
    "api: marks tests that require API keys",
    "docker: marks tests that require Docker",
    "live: marks tests that make live API calls (deselect with '-m \"not live\"')",
    "deployment: marks deployment-recipe tests (Docker/Helm/Lambda/Cloudflare/probes)",
    "security: marks security tests (sandbox, SSRF, secret-scan, vuln-audit, supply-chain)",
    "expensive: marks long/heavy tests (soak, stress, large GPU runs) — deselect by default in CI",
    "cookbook: marks cookbook snippet tests",
    "reliability: marks reliability-primitive tests (timeouts, retry, circuit, bulkhead)",
    "fuzz: marks fuzz-harness tests (hypothesis-driven)",
]
asyncio_mode = "auto"
timeout = 120
filterwarnings = [
    "error",
    "ignore::UserWarning",
    "ignore::DeprecationWarning",
]

# Coverage - Code Coverage
[tool.coverage.run]
source = ["effgen"]
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__pycache__/*",
    "*/site-packages/*",
    "*/examples/*",
]
branch = true
# parallel=true makes each process write a uniquely-suffixed data file
# (.coverage.<host>.<pid>.<rand>) so concurrent shards never clobber a shared
# SQLite DB; combine them with `coverage combine` before reporting. relative_files
# keeps paths portable across the per-shard temp envs used by the install matrix.
parallel = true
relative_files = true

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "def __str__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.coverage.html]
directory = "htmlcov"

# Pylint - Linter
[tool.pylint.main]
py-version = "3.11"
ignore = ["CVS", "migrations", "tests"]
jobs = 0  # Auto-detect CPUs

[tool.pylint.messages_control]
disable = [
    "C0111",  # missing-docstring
    "C0103",  # invalid-name
    "R0903",  # too-few-public-methods
    "R0913",  # too-many-arguments
    "W0212",  # protected-access
]

[tool.pylint.format]
max-line-length = 100

[tool.pylint.design]
max-args = 10
max-attributes = 15

# Bandit - Security Linter
[tool.bandit]
exclude_dirs = ["/tests", "/examples"]
skips = ["B101", "B601"]  # Skip assert_used and paramiko_calls

# Ruff - Fast Python Linter (alternative to flake8)
[tool.ruff]
line-length = 100
# Must equal the `requires-python` floor above. Several pyupgrade rules are
# version-gated and fire only at a higher target, and their rewrites do not run
# on an older interpreter, so raising the target ahead of the floor would make
# ruff demand syntax the declared floor cannot execute. The floor moved to 3.11
# for 1.0.0, which turns on UP017 (`datetime.UTC`), UP041 (`TimeoutError`) and
# UP042 (`enum.StrEnum`); UP047 (PEP 695 generics) stays off until the floor
# reaches 3.12. tests/unit/test_static_check_ratchet.py keeps the two in step.
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "ISC", # flake8-implicit-str-concat
    "FLY", # flynt (str.join of literals -> f-string)
    "LOG", # flake8-logging
    "T10", # flake8-debugger (no stray breakpoints)
    "YTT", # flake8-2020 (sys.version misuse)
]
ignore = [
    "E501",   # line too long (handled by black)
    "B008",   # do not perform function calls in argument defaults
    "B028",   # stacklevel in warnings.warn (pre-existing)
    "B904",   # raise ... from err (pre-existing, too many to fix in patch)
    "B905",   # zip() without strict= (pre-existing)
    "C901",   # too complex
    "UP037",  # quoted annotations (pre-existing, cosmetic)
    # UP042 wants `class C(str, Enum)` rewritten to `enum.StrEnum`. It became
    # active when the floor moved to 3.11, and it is not a cosmetic rewrite:
    # `str(TaskStatus.RUNNING)` and `f"{TaskStatus.RUNNING}"` change from
    # "TaskStatus.RUNNING" to "running". TaskStatus is public, and the change is
    # silent — no error, just different text in logs, stored records and any
    # caller that formats one. Four classes are affected (PermissionMode,
    # TaskStatus, AlertSeverity, LoadScenario). Migrating them is a deliberate
    # behaviour change that belongs in a release that announces it, not one a
    # lint autofix slips in.
    "UP042",  # str-Enum -> StrEnum: changes str() of a public enum
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]  # Ignore unused imports in __init__.py
# Examples and validation scripts call load_dotenv() before effgen imports so
# env-dependent modules see the keys at import time — the E402 is intentional.
"examples/**/*.py" = ["E402"]
"scripts/**/*.py" = ["E402"]
"validation/**/*.py" = ["E402"]
"tests/conftest.py" = ["E402"]  # sys.path manipulation must precede effgen imports

[tool.ruff.lint.isort]
known-first-party = ["effgen"]

# Pydocstyle - Docstring Checker
[tool.pydocstyle]
convention = "google"
add-ignore = ["D100", "D104", "D105", "D107"]
match = "(?!test_).*\\.py"
match-dir = "^(?!(tests|examples|docs)).*"
