# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# The docs SPA is built into the landing site's public/ directory, so its
# output is generated on every build and never committed.
public/docs/

# TypeScript incremental build state
*.tsbuildinfo

# Local env files
.env
.env.local
.env.*.local

# Editor / OS noise
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
